// /////////////////////////////////////////////////////
// This will toggle the graph editor open and closed //
//////////////////////////////////////////////////////

if (`window -q -ex graphEditor1Window`)
	deleteUI graphEditor1Window;
else
	tearOffPanel "Graph Editor" "graphEditor" true;
